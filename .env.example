# Segurança
SECRET_KEY=troque-por-uma-chave-forte-e-aleatoria

# Banco de dados
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=sua_senha_mysql
DB_NAME=mineracao_espacial_viabilidade

# Usuário admin criado automaticamente no primeiro boot
DEFAULT_ADMIN_NAME=Admin
DEFAULT_ADMIN_EMAIL=admin@mineracao.local
DEFAULT_ADMIN_PASSWORD=Admin@123456

# Aplicação
APP_NAME=Orbital Mining Hub

# Segurança de login
LOGIN_FAIL_LIMIT=5
LOGIN_FAIL_WINDOW_MIN=15

# JWT
JWT_EXPIRE_MINUTES=60

# Histórico de senhas (impede reuso das últimas N senhas)
PASSWORD_HISTORY_DEPTH=5
