from __future__ import annotations

from typing import Any


def publicar_evento(nome_evento: str, payload: dict[str, Any]) -> None:
    # Stub simples para manter baixo acoplamento sem overengineering.
    _ = (nome_evento, payload)
